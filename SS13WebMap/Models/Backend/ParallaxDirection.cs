namespace SS13WebMap.Models.Backend {
    /// <summary>
    /// Represents a direction the page parallax can go in
    /// </summary>
    public enum ParallaxDirection {
        North, East, South, West
    }
}
